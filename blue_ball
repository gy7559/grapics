// 파란 공이 왼쪽에서 부터 오른쪽으로 이동하는 코드입니다. 외른쪽 벽쪽으로 사라지면 다시 왼쪽에서 나옴니다.

void setup(){
  size(500,500);
}
int k;
void draw(){
  background(0);
  ellipse(k=k+1,250,200,200);
  fill(0,0,255);
  if(k>500) k=0 ;
}
